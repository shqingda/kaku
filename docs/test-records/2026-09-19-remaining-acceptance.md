# 2026-09-19 剩余事项补验

基于 v1.1.8 后的 main，Android 实机仍按用户要求暂缓。本轮不改产品实现，不另发二进制版本。

## 自动化结果

移动端 Jest/RNTL：31 个套件、137 条测试通过；TypeScript 检查通过。本轮新增 7 条，保留真实组件、QueryClient、业务 Hook 和解析器，在网络与存储边界替换外部依赖：

- `collection-screen-flow.test.tsx`：搜索自动读取后续分页并命中，清空后返回浏览缓存且不重复请求；离线离开/返回页面，恢复联网后继续读取。
- `reply-composer.test.tsx`：关闭/重开和完整卸载/挂载恢复草稿；发送失败保留、重试成功仅清理当前草稿；账号切换隔离；旧账号迟到成功不关闭新账号编辑器或擦除内容。
- `history-upgrade.test.tsx`：真实两个 Provider 和存储/响应解析器串联；旧无归属搜索与浏览历史不导入、不上传且原样保留；两个账号各自从云端恢复，关闭同步后重新挂载从本地恢复。

组件重新挂载不等同于设备杀进程。替身 HTTP 成功/失败不等同于 Bangumi 真实发帖。上述自动化没有向公开讨论发送测试内容。

## iOS 模拟器

设备：iPhone 18 Pro / iOS 27.0，开发客户端 `com.shqingda.kaku.debug`，Maestro + simctl；本轮没有可调用的 Argent 工具。

- 收藏：滚动后进入条目详情，点击页面“返回”后原条目仍在原位置可见；再滚回搜索框输入该条目标题，能得到唯一结果；点击 iOS 原生清除按钮后恢复完整列表，流程断言及截图核对通过。非首屏指列表初始视口外，后续网络分页命中由上面的确定性组件测试验证。
- 回复草稿：空草稿输入本机标记后关闭/重开，截图中内容保留；停止进程再启动，重新进入同一章节的回复框，截图中仍保留标记。没有点击发送；验收后已通过“丢弃草稿→放弃”清理本次本机标记。
- 开发客户端：本地重建成功后，停止进程再启动，首页账户入口可见。

首次运行旧开发包曾发生 ExpoFabricView 初始化 SIGTRAP。重建时发现 `ios/.xcode.env.local` 指向已移除的 Node 26.8.2，已在本机改为 `/opt/homebrew/bin/node`，该生成配置不提交。新开发包编译安装通过；不能仅凭一次重试断言旧崩溃根因已确定。

执行中发现两个测试编排问题：Maestro 的 iOS `back` 没有触发页面返回，改用已有“返回”无障碍标签后通过；安装新包打断了一次清空流程，该次不计通过。冷启动后紧接 dev-client 重载的搜索流程也失败，首页冷启动断言本身通过，不能把整个章节流程计作通过。

回复输入框的原生标签组合为“回复内容 + 占位文字”，需要正则匹配。弹层重开后有一次 Maestro 无障碍树只剩应用根节点和开发工具，虽然截图中草稿内容正确，文本断言仍失败；未将其记为完整 Maestro 流程通过，已在 TODO 保留无障碍复核。之后独立的停止进程→启动→章节→回复框路径通过，并截图核对恢复内容。

本机证据目录（不上传包含当前账号的截图）：`~/.maestro/tests/2026-09-19_122338`（收藏返回与搜索）、`2026-09-19_122828`（草稿重开截图/无障碍树异常）、`2026-09-19_122926`（进程重启恢复）、`2026-09-19_123034`（草稿清理；收藏因保留搜索词造成重复输入未通过）、`2026-09-19_123133`（先清空已有搜索词，输入→命中→清空完整通过）。

## 仍需设备或外部条件

- 真机断网、跨业务页面与系统缓存恢复；两台设备推送互不影响。
- 可发言测试账号和测试讨论，用于真实发送成功/失败、多账号授权闭环。
- VoiceOver、系统减弱动效/透明度；同设备性能重新采样。
- 商店凭据、production 构建与演示账号截图。

剩余范围继续维护在根目录 `TODO.md`。
