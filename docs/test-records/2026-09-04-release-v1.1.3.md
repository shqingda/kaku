# 发版验证记录：v1.1.3

> 日期：2026-09-04
> 方式：本地构建（`scripts/build-split-apks.sh v1.1.3 release`，不消耗 EAS 额度）
> 结果：发布成功，https://github.com/shqingda/kaku/releases/tag/v1.1.3

---

## 步骤与结果

| 步骤 | 结果 |
| --- | --- |
| 提升版本、撰写说明、同步 App 内更新日志（`e45d799`） | ✅ changelog 测试 7 项通过 |
| 脚本自动 push main（`ee6121e..e45d799`） | ✅ |
| `expo prebuild` + gradle `assembleRelease`（arm64-v8a） | ✅ 约 9 分钟 |
| tag `v1.1.3` + Release 发布 | ✅ 非草稿 |
| 资产 `kaku-release.apk` | ✅ 58 MB（60,989,911 字节），debug 签名 |
| Release 说明（`scripts/release-notes.md`，中文短句） | ✅ 含「v1.1.2 可直接覆盖安装」 |

## 遇到的问题

- 本机 `gh` 未登录。用 git credential 写入 `gh auth login --with-token` 后继续。
- 构建前 unset 了代理环境变量，避免 `git push` / `gh release` 挂起。
- 无 Android 设备连接（`adb devices` 为空），最终安装包的冷启动烟测未做。

## 后续验证

最终 release APK 安装烟测仍在 `TODO.md`：冷启动、登录/深链、通知权限、Sentry、覆盖安装。
